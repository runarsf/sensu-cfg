type: CheckConfig
api_version: core/v2
metadata:
  name: public-ip
  labels:
    sensu.io/workflow: sensu-flow
spec:
  command: http-check --url https://cloud.runarsf.dev
  handlers:
  - telegram-handler
  interval: 900
  publish: true
  subscriptions:
  - system
  runtime_assets:
  - http-checks
